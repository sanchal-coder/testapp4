<div class="mx-auto max-w-screen-lg px-4 py-8 sm:px-8">
  <div class="flex items-center justify-between pb-6">
    <div>
      <h2 class="font-semibold text-gray-700">Patients</h2>
      <span class="text-xs text-gray-500">View details of registered patients</span>
    </div>
    <p style="color: green"><%= notice %></p>
    <div class="flex items-center justify-between">
      <div class="ml-10 space-x-8 lg:ml-40">
        <%= link_to "New Patient", new_patient_path, class: "flex items-center gap-2 rounded-md bg-blue-600 px-4 py-2 text-sm font-semibold text-white focus:outline-none focus:ring hover:bg-blue-700"  %>
      </div>
    </div>
  </div>
  <div class="overflow-y-hidden rounded-lg border">
    <div class="overflow-x-auto">
      <table class="w-full">
        <thead>
          <tr class="bg-blue-600 text-left text-xs font-semibold uppercase tracking-widest text-white">
            <th class="px-5 py-3">ID</th>
            <th class="px-5 py-3">Name</th>
            <th class="px-5 py-3">Age</th>
            <th class="px-5 py-3">Contact Info</th>
            <th class="px-5 py-3">Address</th>
            <th class="px-5 py-3">Actions</th>
          </tr>
        </thead>
        <tbody class="text-gray-500">
          <% @patients.each do |patient| %>
          <tr>
            <td class="border-b border-gray-200 bg-white px-5 py-5 text-sm">
              <p class="whitespace-no-wrap"><%= patient.id %></p>
            </td>
            <td class="border-b border-gray-200 bg-white px-5 py-5 text-sm">
              <p class="whitespace-no-wrap"><%= patient.name %></p>
            </td>
            <td class="border-b border-gray-200 bg-white px-5 py-5 text-sm">
              <p class="whitespace-no-wrap"><%= patient.age %></p>
            </td>
            <td class="border-b border-gray-200 bg-white px-5 py-5 text-sm">
              <p class="whitespace-no-wrap"><%= patient.contact_info %></p>
            </td>
            <td class="border-b border-gray-200 bg-white px-5 py-5 text-sm">
              <p class="whitespace-no-wrap"><%= patient.address %></p>
            </td>
            <td class="border-b border-gray-200 bg-white px-5 py-5 text-sm">
              <%= link_to 'Show', patient, class: 'text-blue-600 hover:underline' %> |
              <%= link_to 'Edit', edit_patient_path(patient), class: 'text-blue-600 hover:underline' %> |
              <%= button_to 'Delete', patient, method: :delete, data: { confirm: 'Are you sure?'}, class: 'text-red-600 hover:underline' %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="flex flex-col items-center border-t bg-white px-5 py-5 sm:flex-row sm:justify-between">
      <span class="text-xs text-gray-600 sm:text-sm"> Showing <%= @patients.count %> Entries </span>
    </div>
  </div>
</div>